---
import MBJLogo from "../assets/images/logo-mbj.svg";
import MBJSerwisLogo from "../assets/images/logo-mbj-serwis.svg";
import { Theme } from "../layouts/index.astro";

interface Props {
	type: Theme;
}
const { type } = Astro.props;
---

{Theme.Administracja === type ? <MBJLogo /> : <MBJSerwisLogo />}
